/**
 * @module config/weapon-data
 * 무기체계 타입 + 능력(capability) + 관계(relation) 선언적 정의
 * SSOT: 모든 무기체계 파라미터의 단일 소스
 *
 * Phase 1.2에서 구현 예정
 */

/** @type {Object<string, Object>} 사수 타입 레지스트리 */
export const SHOOTER_TYPES = Object.freeze({});

/** @type {Object<string, Object>} 센서 타입 레지스트리 */
export const SENSOR_TYPES = Object.freeze({});

/** @type {Object<string, Object>} C2 타입 레지스트리 */
export const C2_TYPES = Object.freeze({});

/** @type {Object<string, Object>} 위협 타입 레지스트리 */
export const THREAT_TYPES = Object.freeze({});

/** @type {Object<string, Object>} 토폴로지 관계 */
export const TOPOLOGY_RELATIONS = Object.freeze({});
